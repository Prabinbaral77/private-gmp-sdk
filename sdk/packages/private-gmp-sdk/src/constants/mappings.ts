/**
 * Defaults for the generic mapping reader. The Aleo RPC base mirrors the host
 * `AleoWalletProvider` is constructed with in the implementation app
 * (`ALEO_API_HOST ?? 'https://api.provable.com/v2'`).
 */
export const DEFAULT_ALEO_RPC_URL = 'https://api.provable.com/v2';
